import '../App.js' ;


class LinkedList{
	constructor(){
		this.head=null;
		this.tail=null;
		this.currentNode=null;

	}

// first ⇒ position to the first node

// last ⇒ position to the last node

// next ⇒ move to the next node

// previous ⇒ backup one node (? hmm… how are we going to do this?)

// insert ⇒ inserts a new node after the current node

// delete 

}
export default LinkedList